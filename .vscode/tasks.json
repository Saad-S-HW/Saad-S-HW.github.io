{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "OCaml Build and Run",
            "type": "shell",
            "command": "ocamlc", // Replace with the actual OCaml compiler command (e.g., ocamlc or ocamlopt)
            "args": ["-o", "output_program", "test.ml"], // Replace "output_program" with your desired output program name
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
